<%= form_for(@price) do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% @price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :created_at %><br />
    <%= f.datetime_select :created_at %>
  </div>
  <div class="field">
    <%= f.label :available_until %><br />
    <%= f.date_select :available_until %>
  </div>
  <div class="field">
    <%= f.label :is_limited %><br />
    <%= f.check_box :is_limited %>
  </div>
  <div class="field">
    <%= f.label :manufacturer_id,currency_id %><br />
    <%= f.text_field :manufacturer_id,currency_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
